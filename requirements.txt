# ==========================================================================
# Master Thesis - Production App Dependencies
#   - André Filipe Gomes Silvestre, 20240502
#
# LISBOA: Lisbon Itinerary System Based On AI
# A Multi-Agent Approach for Personalized Tourism and Urban Mobility in Lisbon
#
# Installation: pip install -r requirements.txt
#
# This file is intentionally limited to the Streamlit runtime. Scraping-only
# and eval-only packages stay in pyproject.toml optional extras so Community
# Cloud does not try to compile unnecessary Linux dependencies during deploy.
# ==========================================================================

# -----------------------------------------------------------------------------
# Core LangChain Framework
# -----------------------------------------------------------------------------
langchain==1.2.10             # Latest validated LangChain package for this repository as of 2026-03, on Python 3.10
langchain-core==1.2.18        # Core runtime aligned with LangChain 1.2 and LangGraph 1.0.10
langgraph==1.0.10             # Latest validated LangGraph release for this repository as of 2026-03, used by the worker subgraphs
langgraph-prebuilt==1.0.8     # Pin 2026-04 stable prebuilt build; newer 1.0.x releases can fail importing ExecutionInfo/ServerInfo on cloud deploys
langchain-text-splitters==1.1.1  # Text splitting utilities aligned with the LangChain 1.2 stack

# -----------------------------------------------------------------------------
# LLM Provider Integrations
# -----------------------------------------------------------------------------
langchain-openai==1.1.11      # OpenAI GPT models & Azure OpenAI Service

# -----------------------------------------------------------------------------
# Language Detection (for multilingual input handling)
# -----------------------------------------------------------------------------
langdetect==1.0.9             # ISO-639-1 language detection for non-PT/EN inputs

# -----------------------------------------------------------------------------
# Vector Store and Embeddings (RAG)
# -----------------------------------------------------------------------------
langchain-chroma==1.1.0       # ChromaDB integration for LangChain
langchain-huggingface==1.2.1  # HuggingFace embeddings
langchain-community==0.4.1    # Community integrations
chromadb==1.5.5               # Vector database
sentence-transformers==5.2.3  # Embedding models
transformers==5.3.0           # Backend required by sentence-transformers

# OpenTelemetry (required by chromadb, pinned for compatibility)
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp-proto-grpc==1.40.0
opentelemetry-exporter-otlp-proto-common==1.40.0

# -----------------------------------------------------------------------------
# Data Processing
# -----------------------------------------------------------------------------
pandas==2.3.3                 # DataFrame operations used by municipal/open-data tooling
requests==2.32.5              # HTTP client for API calls
# beautifulsoup4==4.12.3        # HTML parsing for web scraping
# lxml==5.3.0                   # Fast XML/HTML parser
tqdm==4.67.3                  # Progress bars
protobuf==6.33.5              # Protocol Buffers for data serialization
gtfs-realtime-bindings==2.0.0 # GTFS real-time protocol bindings
urllib3==2.6.3                # Explicit transport dependency used by Metro tool
certifi==2026.1.4             # SSL certificate bundle used by Metro tool
cryptography==44.0.1          # X.509 parsing for dynamic Metro TLS chain completion

# Scraping extras such as beautifulsoup4/lxml are kept out of the production
# deploy path because the Streamlit app does not import the webscraping scripts
# at runtime.

# -----------------------------------------------------------------------------
# Document Processing (for RAG ingestion)
# -----------------------------------------------------------------------------
pypdf==6.4.2                  # PDF text extraction
wikipedia==1.4.0              # Wikipedia API wrapper
duckduckgo-search==8.1.1      # DuckDuckGo search tool
ddgs==9.14.2                  # Required by DuckDuckGoSearchAPIWrapper


# -----------------------------------------------------------------------------
# Web Interface
# -----------------------------------------------------------------------------
streamlit==1.56.0             # Interactive web application

# -----------------------------------------------------------------------------
# Monitoring and Evaluation
# -----------------------------------------------------------------------------
langsmith==0.7.16             # LangSmith tracing and evaluation, upgraded for tracing fixes and pytest improvements
pydantic==2.12.4              # Explicitly imported by eval/llm_judge.py

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
python-dotenv==1.2.2          # Environment variable management
tzdata>=2024.2                # IANA timezones for Europe/Lisbon in hosted runtimes
